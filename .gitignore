# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html
coverage-*.html
coverage.out
gosec.json
# Go workspace — go.work is committed for the monorepo (see README.md);
# go.work.sum is a per-developer checksum file that causes merge conflicts
# and is not needed when every module maintains its own go.sum.
go.work.sum

# Dependency directories
vendor/

# Build artifacts
services/mddbd/mddbd
services/mddbd/mddbd-linux
services/mddbd/mddbd.exe
.windsurf/
.claude/
.antigravity/

# Database files
*.db
backup-*.db

# IDE specific files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Air (hot reload) temporary files
tmp/


# Logs
*.log

# Environment files
.env
.env.local
.env.*.local

# Temporary files
*.tmp
*.temp

# Test results and binaries
test/performance-results-*.txt
test/search-benchmark-results-*.txt
test/lorem-*.md
test/protocol-comparison-*.txt
test/database-comparison-*.txt
test/grpc-performance-test
test/grpc-batch-test
test/mysql-benchmark
test/postgres-benchmark
test/mongodb-benchmark
test/couchdb-benchmark
test/grpc-performance-results.txt
test/grpc-batch-performance-results.txt
test/mysql-performance-results.txt
test/postgres-performance-results.txt
test/mongodb-performance-results.txt
test/couchdb-performance-results.txt

# Generated protobuf code for clients (regenerate with: make generate-proto)
# Note: Server proto files (services/mddbd/proto/*.pb.go) are committed to git
clients/python/mddb_client/*_pb2.py
clients/python/mddb_client/*_pb2_grpc.py
clients/nodejs/proto/*.js
clients/go/proto/*.pb.go
services/php-extension/proto/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Node.js
node_modules/
yarn.lock

# Note: package-lock.json should be committed for applications (panel, cli)
# to ensure reproducible builds in CI/CD. Only ignore for libraries.
.windsurf/rules/*.md

# Panel build artifacts
services/mddb-panel/dist/
services/mddb-panel/.env
services/mddb-panel/.env.local
services/mddb-cli/mddb-cli
services/mddbd/mddb
services/mddbd/mddb-server
tools/bench/mddb-bench

# Rust build artifacts
services/mddb-chat/target/

# Local chat config holds real secrets (SEC-007) — track config.example.toml only
services/mddb-chat/config.toml

# Chat widget build artifacts
services/mddb-chat-widget/dist/

# SSG generated output and temp content
dist/
output/
output-custom/
output-test/

# GitHub Action bundled output MUST be committed — Actions are consumed
# directly from the repo path (uses: tradik/mddb/integrations/github-action@v1).
!integrations/github-action/dist/
!integrations/github-action/dist/**

# Chrome extension build artefacts (rebuilt by CI from src/, never committed).
integrations/chrome-extension/node_modules/
integrations/chrome-extension/build/
integrations/chrome-extension/dist/
integrations/chrome-extension/coverage/
integrations/chrome-extension/*.zip

# Grafana datasource plugin — dist/ and zip are release artefacts, not committed.
integrations/grafana-datasource/dist/
integrations/grafana-datasource/build/
integrations/grafana-datasource/coverage/
integrations/grafana-datasource/node_modules/
integrations/grafana-datasource/*.zip
output-test2/
.ssg-output/
content/
docs/pages/
docs/metadata.json

# Wrangler cache
.wrangler/

# Local audit findings — internal issue tracker, never pushed to the repo
/audit/
AUDIT_REPORT.md

# GO-041: the soak tool's binary and its output (CSV + pprof profiles).
# A report belongs in the ticket that asked for it, not in the tree.
tools/bench/soak/soak
tools/bench/soak/mddb-soak
/mddb-soak
soak-report/
