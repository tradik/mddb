[package]
name = "mddb-chat"
version = "0.1.0"
edition = "2024"
description = "Chat support server for MDDB — embeddable AI assistant with RAG"
license = "BSD-3-Clause"
repository = "https://github.com/tradik/mddb"

[dependencies]
# Web framework
axum = { version = "0.8", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
tower = "0.5"
tower-http = { version = "0.7", features = ["cors", "trace"] }

# gRPC client
tonic = "0.14"
tonic-prost = "0.14"
prost = "0.14"
prost-types = "0.14"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "1.1"

# HTTP client (LLM calls)
reqwest = { version = "0.13", features = ["json", "stream"] }
futures = "0.3"

# Session management
dashmap = "6"
uuid = { version = "1", features = ["v4"] }

# Security
governor = "0.10"
# Trusted-proxy matching for the rate limiter (SEC-014). Already in the tree
# through reqwest; declared directly because client_ip depends on it.
ipnet = "2.12"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Crypto (webhook signatures)
hmac = "0.13"
sha2 = "0.11"
hex = "0.4"

# Async utilities
tokio-stream = "0.1"
async-trait = "0.1"

# Text processing
regex = "1"

[build-dependencies]
# tonic 0.14 moved prost codegen out of tonic-build into tonic-prost-build.
tonic-prost-build = "0.14"
